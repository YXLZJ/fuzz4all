#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> nums{3, 5, 2, 4};
    
    auto min = [](const vector<int>& v) -> int {
        return *min_element(v.begin(), v.end());
    };
    
    cout << "Minimum element: " << min(nums);
}