#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

template<typename T>
void printVector(const vector<T>& v) {
    for (const auto& e : v) {
        cout << e << " ";
    }
    cout << endl;
}

int main() {
    string s = "Hello, World!";
    vector<string> vs{s};
    
    sort(vs.begin(), vs.end());
    printVector(vs);

    return 0;
}