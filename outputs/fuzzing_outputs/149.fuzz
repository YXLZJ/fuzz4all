#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<string> words = {"apple", "banana", "cherry"};
    
    auto itr = min_element(words.begin(), words.end());
    cout << *itr << endl;

    return 0;
}