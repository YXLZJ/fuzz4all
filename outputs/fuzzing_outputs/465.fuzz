#include <iostream>
#include <vector>

int main() {
    std::vector<double> vctr = {-1.2, -3.4, 5.6};
    double sum = 0;
    
    for (auto& val : vctr) {
        sum += val * static_cast<long>(val + 17);
    }
    
    std::cout << "Sum: " << sum << std::endl;
    
    return 0;
}