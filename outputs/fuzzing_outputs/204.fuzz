#include <iostream>
#include <string>

int main() {
    std::string input;
    std::cout << "Enter a string: ";
    getline(std::cin, input);

    auto reversed = input.rbegin();
    for (auto i : input) {
        std::cout << *reversed++;
    }

    return 0;
}