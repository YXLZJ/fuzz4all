#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<std::string> words = {"ramuin", "nam", "uto", "a", "to", "rr+tlamd('< e"};
    
    // Use new C++ features to sort the vector in reverse order and print each word on a separate line.
    std::sort(words.rbegin(), words.rend());
    for (const auto& word : words) {
        std::cout << word << '\n';
    }
    
    return 0;
}