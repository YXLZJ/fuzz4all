#include <iostream>
#include <ranges>
using namespace std;

template<typename T>
void print(const ranges::range<T>& r) {
    for (const auto& elem : r) {
        cout << elem << " ";
    }
}

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    print(arr);
    return 0;
}