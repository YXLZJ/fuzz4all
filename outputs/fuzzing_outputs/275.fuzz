#include <iostream>
#include <string>
using namespace std;

int main() {
    string input = "Hello, World!";
    auto reverse_iterator = input.rbegin();
    
    for (auto it = reverse_iterator; it != input.rend(); ++it) {
        cout << *it;
    }
    
    return 0;
}