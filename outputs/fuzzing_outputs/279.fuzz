#include <iostream>
#include <vector>
#include <numeric>
int main() {
    std::vector<double> numbers = {1.4, 5.6};
    double sum = std::accumulate(numbers.begin(), numbers.end(), 0.0);
    std::cout << "Sum: " << sum << std::endl;
    return 0;
}