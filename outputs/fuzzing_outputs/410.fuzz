#include <iostream>
#include <string>
using namespace std;

int main() {
    auto ux = [] (auto&&... args) -> string { return concat(args); };
    cout << "slt: ";
    for (const char* str : {"Hello", ",", " World!"}) {
        cout << ux(str, '');
    }
    cout << endl;
}