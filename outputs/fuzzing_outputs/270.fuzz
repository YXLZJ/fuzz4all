#include <iostream>
#include <tuple>
using namespace std;

int main() {
    auto result = [](auto a, auto b) -> decltype(a * b) { return (b - 2); };
    
    cout << "Et twoub:   a, b >> "; cin >> a >> b;
    cout << "au rsul sm({}{" << result(a, b) << "}");
    return 0;
}