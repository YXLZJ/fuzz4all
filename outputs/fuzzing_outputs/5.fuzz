#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
using namespace std;

int main() {
  vector<int> v = {1,2,4};
  
  sort(v.begin(), v.end());
  
  int sum = accumulate(v.begin(), v.end(), 0);
  
  cout << "Sum of elements: " << sum << endl;
}