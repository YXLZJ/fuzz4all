#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <sstream>

int main() {
    constexpr auto words = std::vector<std::string>{ "apple", "banana", "cherry" };
    
    for (const auto& word : words) {
        std::cout << word << ' ';
    }

    return 0;
}