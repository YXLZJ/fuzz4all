#include <iostream>
#include <string>
#include <vector>

int main() {
    std::vector<std::string> words = {"hello", "world"};
    for (auto &word : words) {
        word += '!';
    }
    
    for (const auto &word : words) {
        std::cout << word << '\n';
    }
}