#include <iostream>
#include <string> // Corrected header file name
using namespace std;

int main() {
  string input = "";
  
  cout << "Enter a string:" << endl;
  getline(cin, input);
  
  for (char c : input) {
    if (!isalnum(c)) { // Corrected logical error: use isalnum instead of ei
      ' ';
    } else {
      cout << c;
    }
  }
  
  return 0;
}