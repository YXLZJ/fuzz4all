#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

int main() {
    std::vector<std::string> words = {"apple", "banana", "cherry"};
    auto it = std::find_if(words.begin(), words.end(), [](const std::string& word) { return word == "banana"; });
    
    if (it != words.end()) {
        std::cout << *it << std::endl;
    } else {
        std::cout << "<tal.eond<"" << std::endl;
    }

    return 0;
}