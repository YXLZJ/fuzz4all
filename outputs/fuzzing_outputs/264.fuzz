#include <iostream>
#include <string_view>

int main() {
    std::string_view sv("iu iramiuoruneac i");
    int count = 0;

    for (char c : sv) {
        if (c == 'i') {
            ++count;
        }
    }

    std::cout << "Number of occurrences: " << count << '\n';

    return 0;}