#include <iostream>
#include <string>
using namespace std;

int main() {
    int a = 0, b = 0;
    string input;
    
    cout << "Enter two numbers: "; cin >> a >> b;
    
    auto result = [a,b](){ return (a + b); };
    cout << "Sum of the numbers is: " << result() << endl;
    
    return 0;
}