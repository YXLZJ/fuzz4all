#include <iostream>
#include <vector>
#include <string>

int main() {
    std::vector<std::string> vec = {"apple", "banana", "cherry"};
    for (size_t i = 0; i < vec.size(); ++i) {
        std::cout << vec[i] << '\n';
    }
    return 0;
}