#include <stdio.h>

int main() {
    int n, i;
    printf("Enter a number: ");
    scanf("%d", &n);
    if (n <= 0) goto end;
    for (i = 1; i <= n; i++) {
        printf("%d\n", i);
    }
end:
    return 0;
}