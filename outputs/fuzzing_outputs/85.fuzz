#include <iostream>
#include <vector>

int main() {
    std::vector<std::string> vec = {"Hello", "World"};
    
    for (const auto& str : vec) {
        std::cout << str << ' ';
    }
    
    return 0;
}