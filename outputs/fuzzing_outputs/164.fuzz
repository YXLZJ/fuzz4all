#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<std::string> words = {"apple", "banana", "cherry", "date", "elderberry"};
    
    auto sort_words = [](const std::string& a, const std::string& b) { return a.length() < b.length(); };
    std::sort(words.begin(), words.end(), sort_words);
    
    for (const auto& word : words) {
        std::cout << word << " ";
    }
    
    return 0;
}