#include <iostream>
#include <string_view>

int main() {
    std::string_view str = "Hello, World!";
    for (std::size_t i = 0; i < str.length(); ++i) {
        if (str[i] == 'o') {
            std::cout << "Found o at index: " << i << '\n';
        }
    }
    return 0;
}