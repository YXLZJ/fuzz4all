#include <iostream>
#include <algorithm>
int main() {
    std::string hello = "Hello World";
    auto reverse_iterator = std::rbegin(hello);
    
    for (auto it = reverse_iterator; it != std::rend(hello); ++it) {
        std::cout << *it;
    }
}