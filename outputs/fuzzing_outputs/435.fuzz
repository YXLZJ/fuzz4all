#include <iostream>
#include <vector>
using namespace std;

template<typename T>
void print(const vector<T>& v, const char* delim = " ") {
    for (size_t i = 0; i < v.size(); ++i) {
        if (i > 0) cout << delim;
        cout << v[i];
    }
}

int main() {
    vector<int> numbers{1, 2, 3, 4, 5};
    print(numbers);
    return 0;
}