#include <iostream>
#include <optional>
#include <tuple>

int main() {
    std::optional<std::tuple<int, int>> opt_tup = std::make_optional(std::make_tuple(1, 3));
    
    if (opt_tup) {
        auto [v] = *opt_tup;
        std::cout << v << '\n';
    }

    return 0;
}