#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

int main() {
    std::string input = "Hello World";
    auto words = split_words(input);
    
    for (const auto& word : words) {
        if (!word.empty()) {
            std::cout << word << ' ';
        }
    }

    return 0;
}
