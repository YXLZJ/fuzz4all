#include <iostream>
#include <string>

int main() {
    std::string str = "";
    while (std::getline(std::cin, str)) {
        if (!str.empty()) {
            std::cout << str << '\n';
        }
    }
    return 0;
}