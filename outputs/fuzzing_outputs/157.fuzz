#include <iostream>
#include <string_view>

int main() {
    std::string_view sv("Hello, World!");
    auto length = [&](){ return sv.size(); };
    std::cout << "Length of string: " << length() << '\n';
    return 0;
}