#include <iostream>
#include <string>

int main() {
    std::string input;
    std::cout << "Enter a string: ";
    getline(std::cin, input);
    
    auto length = input.length();
    std::cout << "Length of the entered string is: " << length << '\n';
    
    return 0;
}