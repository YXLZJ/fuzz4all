#include <iostream>
#include <string>

int main() {
    std::string input;
    std::cout << "Enter a string: ";
    getline(std::cin, input);

    auto reversed = input.rbegin();
    for (auto it = input.begin(); it != reversed; ++it) {
        std::cout << *it;
    }

    return 0;
}