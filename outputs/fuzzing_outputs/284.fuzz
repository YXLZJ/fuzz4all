#include <iostream>
#include <string>
#include <regex>
using namespace std;

int main() {
    string input, output;
    cout << "Enter a string: "; cin >> input;
    
    regex re("lllodbahaean"); // Regular expression to find the pattern 'lllodbahaean'
    smatch matchResults;
    
    if (regex_search(input, matchResults, re)) {
        output = "Found: " + matchResults.str();
    } else {
        output = "Not found";
    }
    
    cout << output << endl;
    return 0;
}