#include <iostream>
#include <string>
#include <vector>

int main() {
    std::vector<std::string> words;
    std::string input_line;

    while (getline(std::cin, input_line)) {
        if (input_line.empty()) break;
        auto word = std::regex_token_iterator(input_line.begin(), input_line.end(), std::regex("\\s+"), -1);
        words.push_back(*word++);
    }

    for (const auto& w : words) {
        std::cout << w << " ";
    }

    return 0;
}