#include <iostream>
#include <string_view>

int main() {
    std::string_view str = "bg routine";
    for (char c : str) {
        if (c == ' ') {
            std::cout << "\n";
        } else {
            std::cout << c;
        }
    }
    return 0;
}