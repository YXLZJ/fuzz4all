#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n = 0;
    vector<int> v(n);
    
    for (auto i : v) cin >> i;
    
    sort(v.begin(), v.end());
    
    auto it = unique(v.begin(), v.end());
    v.resize(distance(v.begin(), it));
    
    cout << "Unique sorted elements: ";
    for (auto i : v) cout << i << ' ';
    return 0;
}