#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<double> v = {1.0, 3.2, 4.9};
    auto it = std::find_if(v.begin(), v.end(), [](const double& a) { return a > 2; });
    
std::cout << "First element greater than 2: " << *it << std::endl;
}