#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
using namespace std;

int main() {
    vector<double> v1 = {-3.5, -2.4, 7.6, 0.9};
    vector<double> v2(v1);
    
    sort(begin(v2), end(v2));
    
    for (auto it : v2) {
        cout << *it;
    }
    
    return 0;
}