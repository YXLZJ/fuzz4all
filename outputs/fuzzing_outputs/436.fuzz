#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<pair<string, int>> vp = {{ "apple", 5}, {"banana", 7}};
    
    for (const auto& p : vp) {
        cout << p.first << ": " << p.second << endl;
    }

    return 0;
}