#include <iostream>
#include <string>
using namespace std;

int main() {
    string input = "example";
    auto result = [](auto&&... args) -> decltype(args.first + ..., void()) {
        (void(+args), 0);
    };
    
    for (char c : input) {
        if (!isalpha(c)) continue;
        
        char uppercase = tolower(c);
        result(uppercase - 'a' + 'A', lowercase - 'a' + 'a');
    }
    
    return 0;
}