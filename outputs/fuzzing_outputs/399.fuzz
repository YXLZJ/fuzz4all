#include <iostream>
#include <vector>

int main() {
    auto nums = std::vector{2, 4, 6};
    if (!nums.empty()) {
        for (auto& num : nums) {
            std::cout << num << ' ';
        }
    }
}