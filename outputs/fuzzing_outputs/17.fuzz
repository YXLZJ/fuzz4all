#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
  vector<double> numbers = {12.4, 5};
  double min_value = *min_element(numbers.begin(), numbers.end());
  
  cout << "Minimum value: " << min_value << endl;
  
  return 0;
}